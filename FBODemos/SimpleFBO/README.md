# Simple FBO

This demo shows how to use a framebuffer object (FBO) to render to a texture. This works with two passes, the first pass renders a teapot to a texuture. The 2nd pass then renders this teapot as the texture to the quad and sphere.
